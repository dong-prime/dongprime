# Copy to .streamlit/secrets.toml (NEVER commit the real file).
# On Streamlit Community Cloud, paste these in the app's "Secrets" settings.

SUPABASE_URL = "https://ovxhiclfsboqhmyfgfip.supabase.co"
# service_role key (Supabase → Project Settings → API). Server-side ONLY.
SUPABASE_SERVICE_KEY = "your-service-role-key"
# One shared admin password to open the dashboard.
ADMIN_PASSWORD = "choose-a-strong-password"
